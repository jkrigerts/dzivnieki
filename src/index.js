import React from "react";
import ReactDOM from "react-dom/client";
import AppSt from "./AppSt";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <AppSt />
  </React.StrictMode>
);
